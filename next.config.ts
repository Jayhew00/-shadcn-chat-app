import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['shadcn-chat-oor561nld-hewmannxuan-gmailcoms-projects.vercel.app'],
    unoptimized: true
  },
  reactStrictMode: true,
  swcMinify: true,
};

export default nextConfig;
